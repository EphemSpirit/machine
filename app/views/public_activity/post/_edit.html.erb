<% if activity.trackable.present? %>
  <div class="ui feed">
    <div class="label activity-label">
      <i class="edit icon"></i>
      <%= link_to activity.trackable.author.name, user_path(activity.trackable.author) %> edited a <%= link_to "post", activity.trackable %>!
    </div>
  </div>
<% end %>
