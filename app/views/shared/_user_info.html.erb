<div class="ui left floated card">
  <% if @user.profile.profile_pic.attached? %>
    <%= image_tag @user.profile.display_image %>
  <% end %>
  <div class="content">
    <div class="header">
      <%= @user.name %>
    </div>
    <p><%= @user.profile.bio %></p>
  </div>
  <div class="extra content">
    <div class="ui vertical buttons">
      <div class="ui basic green button">
        <%= link_to "Friends: ", user_friendships_path(@user) %><%= "#{total_friends(@user)} total" %>
      </div>
      <div class="ui basic green button">
        <%=  button_to "Send Friend Request", user_requests_path(user_id: @user.id, request: { sender_id: current_user.id, receiver_id: @user.id }), class: "ui green button" %>
      </div>
    </div>
  </div>
</div>
